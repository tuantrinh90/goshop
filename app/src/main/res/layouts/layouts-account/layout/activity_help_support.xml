<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:focusableInTouchMode="true"
    android:focusable="true"
    tools:openDrawer="start">

    <include
        layout="@layout/layout_help_support"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <android.support.design.widget.NavigationView
        android:id="@+id/navigation_slide_menu"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:background="@android:color/white"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:itemTextColor="@drawable/selector_menu_color"
        app:itemIconTint="@drawable/selector_menu_color"
        app:itemBackground="@drawable/selector_menu_item_color"
        app:theme="@style/menuTextStyle"
        app:headerLayout="@layout/layout_slide_menu_header"
        app:menu="@menu/menu_main_drawer"/>


</android.support.v4.widget.DrawerLayout>
